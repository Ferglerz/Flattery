// Surround FFT Flattener - Constants
// Keep this project self-contained and modular.

@init

// Math / safety (use REAPER built-in eps constant)

// Limits (compile-time style knobs)
SFF_MAX_FFT_SIZE = 8192; // must be power of two; memory sized for this max
SFF_MAX_MEDIAN_RADIUS = 25; // caps radius-aware median window
SFF_MAX_MEDIAN_WIN = (SFF_MAX_MEDIAN_RADIUS * 2 + 1);

// Display / UI (allocate for max FFT so UI can show all positive bins)
SFF_MAX_DISPLAY_BINS = SFF_MAX_FFT_SIZE * 0.5;

// Common numeric constants (avoid magic numbers)
SFF_STEREO_AVG_FACTOR = 0.5;  // Factor for stereo averaging: 0.5 * (L + R)
SFF_RATE_OF_CHANGE_THRESHOLD_DB = 1.0;  // Threshold for rate of change scaling (dB)
SFF_MIN_MAGNITUDE_THRESHOLD = 0.0001;  // Minimum magnitude for stable calculations
SFF_MS_TO_SEC = 0.001;  // Milliseconds to seconds conversion factor

// String slots: 0-49 reserved (workspace rule). Use 50+.
#sff_tmp_str = 50;

